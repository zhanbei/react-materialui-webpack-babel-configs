{
  "start": "webpack-dev-server --open --config webpack.dev.js --port 8000 --hot --host 0.0.0.0",
  "build": "webpack --config webpack.prod.js | tee -a logs/build-production_`date +%Y-%m-%d`.txt",
  "test": "echo 'Error: no test specified yet!' && exit 1"
}
